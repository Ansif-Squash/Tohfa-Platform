# TOHFA Launch Readiness Review: Decision Register

**Story Reference:** S-53  
**Audience:** Client Leadership, Product Steering Committee, Lead Architect  
**Purpose:** Comprehensive, evidenced audit of every assumption and design decision made on the client's behalf during Track 1 development, including resolution status for all 13 open contradictions from `docs/rules.md`.

---

## 1. Architectural & Operational Decisions Taken

| # | Domain / Area | Decision Taken | Assumption / Rationale | Cost to Alter in Phase 2 | Authority Required |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **D-01** | **Monetary Arithmetic** | All money calculations use integer minor units (paise/cents) wrapped in a dedicated `Money` value object (`packages/shared-types`). Float arithmetic is strictly prohibited in financial tables. | Prevents IEEE 754 precision drift in wallet balances, cart totals, GST, and payouts. | High (Database migration & schema rebuild if float is required). | Lead Architect / CFO |
| **D-02** | **Farm Anonymity (BR-16)** | Customer catalog API and mobile app operate on aggregated warehouse inventory. Customer payloads NEVER receive farmer identity, farm name, or harvest coordinates. | Enforces non-negotiable PRD farm anonymity to protect supply lines and prevent direct off-platform circumvention. | Very High (Requires redesigning the entire catalog pipeline). | Client CEO / Business Lead |
| **D-03** | **Fair-Price Ceiling (BR-07)** | Hard rejection at produce listing creation if price exceeds the commodity's fair-price ceiling (`PRICE_ABOVE_CEILING` 422). | Prevents speculative price inflation and guarantees buyer fairness. | Low (Config flag to make warning instead of hard error). | Head of Agri Procurement |
| **D-04** | **Counter-Offer State Machine (BR-10, BR-11)** | 24-hour response window with automated lapse-on-expiry, strict 3-round cap, and automatic acceptance when farmer meets warehouse ceiling. | Prevents zombie negotiations and locks terms before Purchase Order creation. | Medium (State machine parameter configuration). | Operations Director |
| **D-05** | **Cart Reservation Lock (BR-22)** | 24-hour stock lock against the warehouse Online allocation bucket with automated release job. | Prevents double-selling inventory while customers complete checkout or wallet top-up. | Low (Adjust `CART_LOCK_DURATION_HOURS` config). | Head of Digital Commerce |
| **D-06** | **Wallet-First Settlement (BR-17)** | Checkout only succeeds if user wallet has sufficient funds. When insufficient, returns 422 `WALLET_INSUFFICIENT` with exact shortfall amount for 1-tap Razorpay top-up recovery. | Removes cash-on-delivery payment friction and eliminates courier reconciliation overhead. | Medium (Adding COD requires courier reconciliation service). | Finance Lead / CEO |
| **D-07** | **Fulfilment Handover OTP (BR-20)** | 4-digit numeric OTP generated on order packaging, held exclusively by customer mobile app, and verified by warehouse admin at physical handover. | Guarantees physical proof of delivery without requiring mobile network coverage at the customer device during handover. | Low (Can be augmented with QR scanning in Phase 2). | Warehouse Logistics Lead |
| **D-08** | **Dual Payout Approval (BR-28, BR-29)** | Farmer payout dues require two distinct admin approvals (Finance Admin + Super Admin) before bank disbursement. | Safeguards platform treasury against single-operator fraud or mistaken disbursements. | Low (Threshold configuration in RBAC). | Compliance Officer |
| **D-09** | **Aadhaar Privacy (BR-33, BR-32)** | Aadhaar numbers encrypted via AES-256 at rest, masked in logs (`+9198****3210` / `****-****-1234`), and accompanied by prominent in-app disclosure. | Fulfills UIDAI regulations and Google Play Data Safety compliance. | Zero (Mandatory legal compliance). | Legal Counsel |
| **D-10** | **Mobile Baselines (S-51)** | Authoritative minimum support baselines locked to Android 8.0 (API 26) and iOS 13.0 with Hermes JS engine. | Maximizes addressable farmer market across rural Tamil Nadu on entry-level 2GB RAM devices. | Medium (Raising baseline drops older handsets). | Mobile Engineering Lead |

---

## 2. Cross-Reference of "Open Contradictions" from `docs/rules.md`

Every row from `docs/rules.md` § "Open contradictions — DO NOT GUESS" is audited below. None are quietly closed without explicit client decision status.

| # | Topic | Requirements v1.0 | Role & Feature Matrix v1.0 | Track 1 Implemented Default | Current Status & Client Risk |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **C-01** | **Audit Scoring Scale (BR-04)** | §2.1: "max 100 points" and tiers `<650 / 650-700 / 700-749 / 750+` in same bullet. | §9 repeats both verbatim. | Thresholds seeded in config; scoring calculation logic deferred; scoring endpoint returns `501 NOT_IMPLEMENTED`. | **OPEN — Requires Client Confirmation.** Low risk for Track 1; must be resolved before Phase 3 Farm Audits module. |
| **C-02** | **Automation (BR-38)** | FR-F06/FR-F07: auto-reminders, suggested treatments, weather risk; §2.2 automatic B2B allocation. | Principle 5: "All data entered manually... No automated processes." | No advisory automation built; only the three rule-enforcement background jobs (cert expiry, counter-offer lapse, cart unlock). | **OPEN — Requires Client Confirmation.** Prevents building advisory engine until manual vs AI policy is signed off. |
| **C-03** | **Fulfilment Model (BR-21)** | §2.3 doorstep OTP at delivery hub, FR-C04 delivery slots, FR-A04 driver performance, Module 14 drivers. | Principle 9: "All orders fulfilled via warehouse pickup"; no driver or fleet features. | Warehouse Pickup + 4-digit handover OTP implemented. Delivery slot schema modeled but unused; no driver entity. | **OPEN — Requires Client Confirmation.** Delivery fleet and routing deferred to Phase 2. |
| **C-04** | **Audit Log Scope for Main Warehouse Admin** | §6.4: `View audit logs` MW = `Own`. | §15: `View system audit logs` MW = `View` (all). | Scoped to `MAIN_WH_ADMIN = own` (Chapter 6 precedence). | **OPEN — Requires Client Confirmation.** Low risk; RBAC grant easily adjusted in `docs/rbac.json`. |
| **C-05** | **Set Warehouse Capacity** | §6.2: SA = Y, TA = Y, MW = Y. | §7: SUPER_ADMIN only; TA = N, MW = N. | SA, TA, and MW may set capacity (Chapter 6 precedence). | **OPEN — Requires Client Confirmation.** Low risk. |
| **C-06** | **Wallet Write Authority for MW / SW** | §6.3 `Wallet management (customers)`: MW = Y, SW = Own. | §8: Manual credit/debit is SA/TA only; MW/SW get view only. | `MAIN_WH_ADMIN = all`, `SUB_WH_ADMIN = own` (Chapter 6 precedence). | **OPEN — HIGH PRIORITY for Client Confirmation.** Money-moving permission should be restricted to SA/TA if strict audit is required. |
| **C-07** | **Bank Refunds for MW / SW** | §6.3 `Process returns (RMA)`: MW = Y, SW = Y (blanket). | §6: Bank refund SA/TA only; MW/SW limited to wallet refunds. | Bank refunds strictly restricted to SA/TA; MW/SW limited to wallet credit adjustments. | **OPEN — Requires Client Confirmation.** Protects bank cash outflow. |
| **C-08** | **Integration Keys & Raw DB Export** | §6.4 + FR-A01: SUPER_ADMIN manages integration keys and has emergency raw DB export. | §15 + Principle 11: Both are infra-level (EXT); no in-app UI. | Permissions retained in RBAC (SA-only); no in-app web UI built (handled via Azure portal & CLI). | **OPEN — Requires Client Confirmation.** Recommended to keep at infrastructure level. |
| **C-09** | **COD vs Wallet-First (BR-17)** | §2.3 wallet-first; FR-C04 mentions COD; Ch.11 Decision 17 asks if COD is supported. | Principle: Wallet-first checkout enforcement; no COD row. | Wallet-first only; COD is rejected. | **OPEN — Requires Client Confirmation.** Client must confirm if COD is permanently excluded. |
| **C-10** | **B2B / Horeca Allocation Source (BR-13)** | §2.2: Automatic from consolidated inventory. | §7: `Manage B2B/Horeca Manual allocation`, SA/TA full. | B2B/Horeca endpoints return `501 NOT_IMPLEMENTED` in Track 1; only Online channel is active. | **OPEN — Requires Client Confirmation.** Sized for Phase 2. |
| **C-11** | **Pickup OTP Disclosure (BR-20)** | FR-C05: "4-digit OTP shared with warehouse staff". | §6: `OTP shared with warehouse staff` = False for MW/SW and Customer. | OTP is customer-held; verifying admin inputs customer's OTP; admin never sees OTP in advance. | **RESOLVED by Architecture.** Customer-held OTP is the only secure cryptographic design. |
| **C-12** | **Farmer Approval by Farmer Admin** | §6.1 `Approve farmer applications`: FA = View. | §2: `Approve farmer application` FA = N, separate view queue. | FA has read-only view access; approval requires Super Admin / Tenant Admin. | **RESOLVED.** Documented and verified in RBAC tests. |
| **C-13** | **Object Storage Vendor** | Ch.7/9: Azure Blob or Cloudinary. | §15: AWS S3 or Cloudinary. | Storage access abstracted behind pluggable `StorageAdapter` interface; Azure Blob + Local mock implemented. | **RESOLVED by Architecture.** Zero vendor SDK leak into domain services. |
